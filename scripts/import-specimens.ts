import { config } from 'dotenv';
config();

async function importBatch() {
  console.log('Iniciando preparación para el ingreso masivo de productos...');
  // Aquí puedes integrar la lectura de tus 100 productos (JSON/CSV) hacia tu base de datos o Cloudinary
}

importBatch();
