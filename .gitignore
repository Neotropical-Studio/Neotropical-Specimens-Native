# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js / build
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# testing / coverage
/coverage

# env files (NUNCA commitear secretos; .env.example sí se versiona)
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# deploy / vercel
.vercel

# Supabase CLI (estado/caché local del link, nunca compartir)
.branches
supabase/.temp
supabase/.branches

# misc
.DS_Store
*.pem

# claude code (config personal/local, no compartir)
.claude/settings.local.json
\]+
scripts/.venv/
.env*

# Estudio automático (solo .gitkeep versionado)
hot_folder/*
!hot_folder/.gitkeep
procesados_finales/*
!procesados_finales/.gitkeep
revision_humana/*
!revision_humana/.gitkeep
auto_studio.log
ingesta.log
ingesta_errores.log
